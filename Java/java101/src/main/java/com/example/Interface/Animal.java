package com.example.Interface;

public interface Animal {

    // Interfaces are abstract classes. They are used to group related methods with empty bodies. 
    // If a class implements an interface, it must implement all it's methods and define their bodies. 
    
    // Methods
    public void animalSound();
    public void eat();
}