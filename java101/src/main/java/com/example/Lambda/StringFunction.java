package com.example.Lambda;

// This is a functional interface. A functional interface is an interface with only one functional method. 
// A functional method is a method without a body. 
public interface StringFunction {
    String run(String str);
}
